'use strict';

class Map {
    constructor(guid, id, icon, thumbnail, type, state, status) {
        this._guid = guid;
        this._id = id;
        this._icon = icon;
        this._thumbnail = thumbnail;
        this._type = type;
        this._state = state;
        this._status = status;
    }
}