'use strict';

class Map {
    constructor(guid, name, icon, thumbnail, type, state, status) {
        this._guid = guid;
        this._name = name;
        this._icon = icon;
        this._thumbnail = thumbnail;
        this._type = type;
        this._state = state;
        this._status = status;
    }
}